# Argument-Parser-using-Python

Running program for getting all supported argument:
      
      python helper.py -h

Running program with arguments:

       python helper.py -n Abhi -pn demo.py -arg1 /bin/usr -arg2 A
